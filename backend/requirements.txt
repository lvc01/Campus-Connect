# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
gunicorn>=22.0.0

# Database
sqlalchemy[asyncio]>=2.0.35
asyncpg>=0.30.0
alembic>=1.13.2

# Validation & settings
pydantic[email]>=2.9.0
pydantic-settings>=2.5.0

# Auth
PyJWT>=2.8.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.9

# File inspection (MIME type validation)
python-magic>=0.4.27

# Structured logging
python-json-logger>=2.0.7

# Error tracking
sentry-sdk>=2.19.0

# Prometheus metrics
prometheus-fastapi-instrumentator>=7.0.0

# Email
aiosmtplib>=3.0.1

# HTTP client (for tests & external calls)
httpx>=0.27.0

# Redis & Rate Limiting
redis>=5.0.0
fastapi-limiter>=0.2.0

# Background jobs
arq>=0.26.0

# Search
meilisearch-python-sdk>=3.0.0

# Storage (S3 / R2)
aioboto3>=13.0.0

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0
aiosqlite>=0.20.0
