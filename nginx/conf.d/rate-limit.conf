# Rate limiting zones — must be in the http context (not inside a server block).
# These are shared across all server blocks that reference them.
limit_req_zone $binary_remote_addr zone=auth:10m rate=10r/m;
limit_req_zone $binary_remote_addr zone=api:10m rate=100r/m;
